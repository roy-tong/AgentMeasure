# Governance

> AgentMeasure 的目标是成为**可被多方采用的开放标准**，而不是某家公司的产品规格。
> 本文件把这一点写成**预先承诺**——在采用者到来之前公布，而不是等有人质疑时才补。
>
> 结构借鉴 PEAC Protocol 的治理与中立性文件（其做法值得学：**把中立性写成可检验的规则，
> 而不是声明**）。同时采纳其诚实纪律：**不声称并不存在的社区。**

## 0. 诚实状态（现状，不美化）

| 项 | 现状 |
|----|------|
| 维护者 | **1 人**（Roy Tong） |
| 外部实现 | **0 个**（尚无第三方实现通过 conformance） |
| 外部 AUP | **0 份**（提案均出自维护者） |
| 治理形态 | 维护者决策（BDFL），**尚未建立 TSC / 工作组** |

**我们不写「由社区共同维护」这类话。** 现在就是一个人在做。
诚实的稀缺读起来是可信；暗示的社区读起来是包装。

## 1. 反捕获承诺（Pre-commitments）

以下承诺在采用者到来**之前**生效，且不因任何单一出资方而改变：

1. **不以独占控制为条件接受资助或采用。** 任何要求「只有出资方能控制标准」的
   条件，一律拒绝。
2. **标准不由单一实现定义。** 参考实现是标准的一个消费者，不是标准本身。
3. **conformance 制品优先于维护者声明。** 判断一个实现是否合规，看向量是否通过，
   不看谁说的。
4. **不设置托管的准入点。** 验证不需要访问任何 AgentMeasure 服务；所有 conformance
   向量与测试数据自包含于本仓库。
5. **治理扩展跟随已验证的外部使用，而不是先于它。** 在有 ≥2 个独立外部实现通过
   conformance 之前，不设立形式化的委员会。

## 2. 自我取消资格的外部证据规则

**什么不算外部证据**（这条约束我们自己）：

- ❌ 维护者自己产出的演示
- ❌ 无法核验的私下声称（「有一家财富 500 强在用」）
- ❌ 把 AgentMeasure 自己造的 fixture 重新贴标签为「外部证据」

**什么算外部证据**：

- ✅ 第三方实现通过公开向量（可指向其仓库）
- ✅ 第三方提交的 fixture 被接收（可指向 PR）
- ✅ 第三方在公开场合引用或反驳我们的结论（可指向链接）

这条规则的作用是：**当我们说「有人用了」，必须能被第三方核实。**

## 3. 密钥与数据

- 维护方**不持有、不托管、不访问**任何实现方的签名密钥
- 不要求上报原始日志；工具默认本地运行且无网络代码
- 若未来出现托管服务，它 MUST NOT 成为合规的必要条件

## 4. 决策机制（AUP）

标准变更通过 **Agent Usage Proposal (AUP)** 流程：

```text
Draft → Discussion → Accepted → Experimental → Stable → Deprecated
```

- 任何人均可提 AUP（GitHub Discussions 的 Proposals 分类）
- 合入 `proposals/` 需要：**2 个独立实现方表示可实施** + 维护者同意
- Experimental 至少运行一个外部验证周期后才可转 Stable
- **已发布的结论不因规则更新被静默改写**；每条公开结论 MUST 能引用产生它的标准版本

## 5. 版本与稳定性承诺

| 类别 | 承诺 |
|------|------|
| 枚举（`registry/vocabularies.yaml`） | 变更须先改 YAML 并跑生成器；CI 校验消费方一致 |
| 指标合同（`registry/metrics.yaml`） | 新增为 minor；删除或改语义为 major，且需 AUP |
| conformance 向量 | 只增不改；旧向量永久有效，新规则用新 ID |
| 文档中的路径与计数 | CI 校验（防止文档指向不存在的文件） |

## 6. 角色（未来，按 §1.5 的次序）

- **Maintainers**：合入权限、版本发布
- **Spec editors**：AUP 的语义完整性
- **Adapter owners**：各自 adapter 的能力矩阵责任

以上角色在满足 §1.5 的条件后设立。当前这些职责由维护者承担。

## 7. 数据治理

- 公开聚合数据的口径变更必须发 Measurement Change Notice
- Identity graph 的认领争议由 maintainers 仲裁，可申诉

## 8. 迁移就绪

所有规范性文本、conformance 向量与测试数据自包含于本仓库，
**设计上支持治理权移交**（stewardship transfer）：若未来成立基金会或工作组，
不需要重新撰写或重新采集任何规范制品。
