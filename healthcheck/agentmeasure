#!/usr/bin/env python3
"""Entry shim — keeps the invocation stable regardless of python layout."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from am_healthcheck.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
